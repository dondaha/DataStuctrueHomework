#include "iostream"
#include <forward_list>
using namespace std;
struct node
{
    float data;
    float *next;
};
int findInDomain(float min, float max, node *head)
{
    int i = 0;
    node *p = head;
    while(p)
    {
        if()
            return i;
        i++;
        p = p->next;
    }
    return -1;
}
int main(){
    node head{0, new node{}};
    for (int i = 0; i < 10; ++i) {
        node * tmp = new node;

    }
}